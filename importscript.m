%% Import data from spreadsheet
% Script for importing data from the following spreadsheet:
%
%    Workbook: C:\Documents\MATLAB\FuelEconomy\dataXLS\2004dat.xlsx
%    Worksheet: Sheet1
%
% To extend the code for use with different selected data or a different
% spreadsheet, generate a function instead of a script.

% Copyright 2015 The MathWorks, Inc.

% Auto-generated by MATLAB on 2015/02/03 10:05:32

%% Import the data
[~, ~, raw] = xlsread('C:\Documents\MATLAB\FuelEconomy\dataXLS\2004dat.xlsx','Sheet1','A2:X2338');
raw(cellfun(@(x) ~isempty(x) && isnumeric(x) && isnan(x),raw)) = {''};
cellVectors = raw(:,[2,3,4,6,8,9,14,17]);
raw = raw(:,[1,5,7,10,11,12,13,15,16,18,19,20,21,22,23,24]);

%% Replace non-numeric cells with NaN
R = cellfun(@(x) ~isnumeric(x) && ~islogical(x),raw); % Find non-numeric cells
raw(R) = {NaN}; % Replace non-numeric cells

%% Create output variable
data = reshape([raw{:}],size(raw));

%% Allocate imported array to column variable names
Year = data(:,1);
MfrName = cellVectors(:,1);
CarLine = cellVectors(:,2);
Car_Truck = cellVectors(:,3);
EngDisp = data(:,2);
Police = cellVectors(:,4);
RatedHP = data(:,3);
Transmission = cellVectors(:,5);
Drive = cellVectors(:,6);
Weight = data(:,4);
Comp = data(:,5);
AxleRatio = data(:,6);
EVSpeedRatio = data(:,7);
AC = cellVectors(:,7);
PRP = data(:,8);
FuelType = data(:,9);
City_Highway = cellVectors(:,8);
HC = data(:,10);
CO = data(:,11);
CO2 = data(:,12);
NOX = data(:,13);
MPG = data(:,14);
Valves_Cyl = data(:,15);
CoastDownTime = data(:,16);

%% Clear temporary variables
clearvars data raw cellVectors R;